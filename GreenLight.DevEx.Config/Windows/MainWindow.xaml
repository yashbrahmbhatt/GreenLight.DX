<Window x:Class="GreenLight.DX.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GreenLight.DX.Windows"
        xmlns:viewModels="clr-namespace:GreenLight.DX.ViewModels"
        xmlns:controls="clr-namespace:GreenLight.DX.Controls"
        d:DataContext="{d:DesignInstance Type=viewModels:ProjectViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <viewModels:ProjectViewModel />
    </Window.DataContext>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/GreenLight.DX.Config;component/ResourceDictionaries/Button.xaml" />
                <ResourceDictionary Source="/GreenLight.DX.Config;component/ResourceDictionaries/TextBox.xaml" />
                <ResourceDictionary Source="/GreenLight.DX.Config;component/ResourceDictionaries/TextBlock.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel
        Background="{StaticResource LightBrushColor}"
        >
        <Menu DockPanel.Dock="Top"
              Background="{StaticResource LightBrushColor}"
              Foreground="{StaticResource DarkBrushColor}"
              >
            <MenuItem Header="_File">
                <MenuItem Header="_New Configuration" Command="{Binding AddConfigurationCommand}" />
                <Separator />
                <MenuItem Header="_Exit" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About" />
            </MenuItem>
        </Menu>

        <ListBox ItemsSource="{Binding Configurations}" DockPanel.Dock="Left" Height="Auto"
                 SelectedItem="{Binding SelectedConfig}"
                 Background="{StaticResource LightBrushColor}">
            <ListBox.ItemTemplate>
                <DataTemplate d:DataType="viewModels:ConfigurationViewModel">
                    <StackPanel Orientation="Horizontal" Width="Auto"
                                Background="{StaticResource LightBrushColor}">
                        <TextBox Text="{Binding Name, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"  
                                 MinWidth="200" Width="Auto" />
                        <Button Content="Delete" Width="Auto" Style="{StaticResource DangerButton}"
                                Command="{Binding DeleteConfigurationCommand}"
                                CommandParameter="{Binding}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <controls:ConfigurationView DockPanel.Dock="Right" DataContext="{Binding SelectedConfig}" />
    </DockPanel>
</Window>